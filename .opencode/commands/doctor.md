---
description: Validate the safe local development prerequisites.
agent: build
---

Run `npm run doctor` from the repository root. Do not print environment values
or inspect secret files. Report each failed prerequisite with the remediation
already provided by the command.
