---
description: Inspect local readiness and nonprod migration parity safely.
agent: build
---

Run `npm run doctor`, then `npm run db:status`. These commands are diagnostic
only. Do not push migrations or connect to production
`yfdelflsheurzaicwayi`. Report whether local prerequisites pass and whether the
linked nonprod project `htketmmhsfmucevvqmxi` is in migration parity.
